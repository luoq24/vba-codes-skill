## 项目简介

本仓库是一个基于 VBA 的 Excel 工具（`svn跨分支合表工具.xlsm`）的源码版本控制工程：

- **业务功能**：用 VBA 比较两个 Excel 工作簿的差异。
- **工程形态**：把 Excel 内的 VBA 代码以文本形式导出到仓库，通过 Git 实现版本控制，可查看每次变更、回退历史版本。

## Agent 核心认知与规则

1. **代码的真正载体是打开的 Excel 文件，仓库文本只是镜像。**
   - VBA 代码实际存在于 `svn跨分支合表工具.xlsm`（二进制文件，不入 Git）。
   - `vba_src/` 下导出的 `.bas` / `.cls` / `.frm` 文本，是便于 Git 对比和阅读的导出物，不是可独立生效的源码。
2. **用户要求修改 VBA 代码时，必须调用 `excel-vba-editor` 技能**：直接编辑已打开的 Excel 实例中的代码，再导出用于 diff 对比。不要直接改动 `.bas` 文本后让用户手动同步，那会绕过 Git 流程。
3. **修改的前置条件：目标 Excel 工作簿必须已在 Excel 中打开**，否则 agent 无法连接实例、无法写回代码。
4. **Git 只跟踪 `vba_src/` 下的文本代码**；`.xlsm` 主文件与 `.temp/` 目录均不入库。
5. **改动完成后**：提醒用户在 Excel 中保存文件，并建议用 `git diff` 复核变更内容后再收尾。

## 目录结构

| 路径 | 说明 |
| --- | --- |
| `svn跨分支合表工具.xlsm` | Excel 主文件（不入 Git，VBA 代码的实际宿主） |
| `vba_src/svn跨分支合表工具/` | 该工作簿导出的 VBA 源码（Git 跟踪） |
| `README.md` | 面向人类用户的使用说明 |
| `.temp/` | 临时文件、临时脚本（Git 忽略） |

**代码定位规则**：工作簿对应 `vba_src/` 下同名子目录，模块对应子目录内的单文件——`vba_src/<工作簿名>/<模块名>.bas`（类模块 `.cls`、窗体 `.frm` 同理）。

## Agent 修改代码的标准流程

1. 通读 `vba_src/svn跨分支合表工具/` 下的现有模块，先理解代码逻辑与结构。
2. 确认 Excel 已打开该工作簿后，调用 `excel-vba-editor` 技能完成修改。
3. 导出最新代码，向用户简要展示 `git diff` 结果，等待用户在 Excel 中确认保存。

## 开发环境

- Excel 2019 以上 / VBA 7.0 / Windows 11 以上
- Python 3.11 以上（供 agent 调用辅助脚本）
